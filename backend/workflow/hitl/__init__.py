"""Human-in-the-Loop (HITL) workflow package.

Provides bidirectional interaction between users and debate agents:
- User → Agent: inject context, ask questions during debate
- Agent → User: request clarification, pause for input
"""
